<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="/style.css">
  <title></title>
</head>

<body>
  <ul>
    <form action="/addTodo" method="post">
      <div>
        <p>TODO List</p>
        <input class="addItem" type="text" name="description" autofocus="on" value="" placeholder="Add Item">
    </form>
    <ul class="incomplete">
      {{#uncompleted}}
        <li>
          <form action="/complete" method="post">
            <button type="submit" name="id" value="{{id}}">Complete</button>
            <label>{{description}}</label>
          </form>
        </li>
      {{/uncompleted}}
    </ul>
    <p>Completed Items</p>
    <ul class="complete">
      {{#completed}}
        <li>{{description}}</li>
      {{/completed}}
    </ul>
    </form>
  </ul>
</body>

</html>
